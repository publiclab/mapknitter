development:
  adapter: mysql2
  username: 
  password: 
  database: mapknitter

production:
  adapter: mysql2
  host: <%= ENV.fetch('DB_HOST') || 'db' %>
  username: <%= ENV.fetch('DB_USER') || 'mapknitter' %>
  password: <%= ENV.fetch('DB_PASS') || 'mapknitter' %>
  database: <%= ENV.fetch('DB_NAME') || 'mapknitter' %>

test:
  adapter: mysql2
  username: mapknitter
  password: mapknitter
  host: db
  database: mapknitter
