[
<% @map.annotations.each_with_index do |annotation, i| %>
  <%= render :partial => 'show.json', :locals => { :annotation => annotation } %><% if i != @map.annotations.length - 1 %>,<% end %>
<% end %>
]