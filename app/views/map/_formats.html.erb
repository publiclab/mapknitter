<p><small><i>(<a href="http://publiclaboratory.org/wiki/map-formats">Learn what to do</a> with these formats)</i></small></p>
<% unless @export.nil? %>
<% if @export.tms || @export.export_type == "nrg" %>
<p>
<% if @export.export_type == "nrg" %>
	<a target="_blank" href="http://archive.publiclaboratory.org/leaflet/?tms=http://mapknitter.org/tms/<%= @map.name %>-nrg/&lat=<%= @map.lat %>&lon=<%= @map.lon %>">Web viewer</a><br />
	<a target="_blank" href="/tms/<%= @map.name %>-nrg/openlayers.html">OpenLayers viewer</a> <small>(refresh your cache if you don't see an up-to-date version)</small><br />
	<a target="_blank" href="/tms/<%= @map.name %>-nrg/">Tiled Map Service (TMS)</a> (OSM-style <a href="/tms/<%= @map.name %>/alt">TMS here</a>)
<% else %>
	<a target="_blank" href="http://archive.publiclaboratory.org/leaflet/?tms=http://mapknitter.org/tms/<%= @map.name %>/&lat=<%= @map.lat %>&lon=<%= @map.lon %>">Web viewer</a><br />
	<a target="_blank" href="/tms/<%= @map.name %>/openlayers.html">OpenLayers viewer</a> <small>(refresh your cache if you don't see an up-to-date version)</small><br />
	<a target="_blank" href="/tms/<%= @map.name %>/">Tiled Map Service (TMS)</a> (OSM-style <a href="/tms/<%= @map.name %>/alt">TMS here</a>)
<% end %>
<% if @export.zip %><a target="_blank" href="/tms/<%= @map.name %>.zip">zipped TMS</a><% end %>
<br />
<a target="_blank" href="/tms/<%= @map.name %>/doc.kml">KML</a> (beta, may not work)<br />
<a target="_blank" href="http://maps.google.com/maps?q=http://cartagen.org/tms/<%= @map.name %>/doc.kml">Google Maps viewer</a> (beta, low res)<br />
</p>
<% end %>
<% if @export.geotiff %>
<p>
<% if @export.export_type == "nrg" %>
	<a href="/export/geotiff/<%= @map.name %>.tif">GeoTIFF</a> (<%= @export.cm_per_pixel %> cm/px, <%= number_to_human_size(@export.size) %>, <%= @export.width %> x <%= @export.height %> px)
<% else %>
	<a href="/export/geotiff/<%= @map.name %>-nrg.tif">GeoTIFF</a> (<%= @export.cm_per_pixel %> cm/px, <%= number_to_human_size(@export.size) %>, <%= @export.width %> x <%= @export.height %> px)
<% end %>
</p>
<% end %>
<% if @export.jpg %>
<p>
<a href="/export/jpg/<%= @map.name %>.jpg">JPG</a> (for faster download and printing)
</p>
<% end %>
<% if false#@export.mbtiles %>
<p>
<a href="/export/mbtiles/<%= @map.name %>.mbtiles">mbtiles</a>
</p>
<% end %>
<% end %>
