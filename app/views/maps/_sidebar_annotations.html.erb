<p>
A new annotation system is under development; for now, <a href="/embed/<%= @map.slug %>?annotations=true">click here</a> to add annotations using the old system.
</p>

<p>To try the new annotations system, click here:</p>

<p><a href="/maps/<%= @map.slug %>/annotate" class="btn btn-lg btn-default">Beta annotation tool</a></p>
